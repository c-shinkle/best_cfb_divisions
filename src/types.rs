pub type Team = &'static str;
pub type TeamPair = (Team, Team);
pub type Division = Vec<Team>;
pub type DivisionPair = (Division, Division);
pub type Conference = [Team];
pub type Pod = Vec<Team>;
pub type PodQuadruple = (Pod, Pod, Pod, Pod);
